import React from "react";
import styles from "./styles.module.css"

export const AppHeader = () => {
  return (
    <header className={styles.header}>
        <h1 className={styles.title}>FoodService</h1>
    </header>
  );
};
